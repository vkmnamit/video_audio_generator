\documentclass[preview]{standalone}
\usepackage[english]{babel}
\usepackage{amsmath}
\usepackage{amssymb}
\begin{document}
\begin{align*}
The time complexity is O(n) because each character is processed exactly once. The space complexity is O(min(m, n)), where m is the size of the character set, due to the storage requirements of the unordered_map.
\end{align*}
\end{document}